# jwt
JWT_SECRET=mock_secret

# postgres
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/metadata_submitter

# ldap
CSC_LDAP_HOST=ldap://mockldap # set to ldap://localhost if running with honcho
CSC_LDAP_USER=cn=admin,dc=csc,dc=fi
CSC_LDAP_PASSWORD=admin

# authentication
OIDC_CLIENT_ID=aud2
OIDC_CLIENT_SECRET=secret_must_be_long
OIDC_URL=http://mockauth:8000
OIDC_SCOPE="openid profile eduperson_entitlement"
# change to http://mockauth:8000 if tests are run from container
OIDC_URL_TEST=http://localhost:8000
OIDC_SECURE_COOKIE=FALSE
# set above to TRUE or remove entirely for production use

# app urls
BASE_URL=http://localhost:5430
REDIRECT_URL=http://localhost:3000

# logging
LOG_LEVEL=DEBUG

# Datacite
DATACITE_API=http://mockdatacite:8001
DATACITE_USER=user
DATACITE_KEY=key
DATACITE_DOI_PREFIX="10.xxxx"

# PID
CSC_PID_URL=http://mockpid:8005
CSC_PID_KEY="mock"

# metax
METAX_URL=http://mockmetax:8002
METAX_TOKEN=token

# rems
REMS_USER=sd
REMS_KEY=demo_key
REMS_URL=http://mockrems:8003

# admin
ADMIN_URL=http://mockadmin:8004

# discovery
REMS_DISCOVERY_URL=https://etsin.demo.fairdata.fi/dataset/
# REMS_DISCOVERY_URL=https://bp-demo.rahtiapp.fi/

# Bigpicture
BP_CENTER_ID=bb

# S3 file service
S3_REGION=us-east-1
STATIC_S3_ACCESS_KEY_ID=test
STATIC_S3_SECRET_ACCESS_KEY=test
SD_SUBMIT_PROJECT_ID=1000
S3_ENDPOINT=http://s3mock:8006
KEYSTONE_ENDPOINT=http://mockkeystone:5001

# Allow unsafe operations during testing.
ALLOW_UNSAFE=TRUE

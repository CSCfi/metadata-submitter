name: Publish Docker image

on:
  release:
    types: [published]
  push:
    branches: [master]

jobs:
  push_to_registry:
    name: Push Metadata Submitter Docker image to Docker Hub
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Login to DockerHub Registry
        run: echo '${{ secrets.DOCKER_PASSWORD }}' | docker login -u '${{ secrets.DOCKER_USERNAME }}' --password-stdin
      - name: Get the version
        id: vars
        run: echo ::set-output name=tag::$(echo ${GITHUB_REF:10})
      - name: Build the tagged Docker image
        if:  ${{ steps.vars.outputs.tag != '/master' }}
        run: docker build . --file Dockerfile --tag cscfi/metadata-submitter:${{steps.vars.outputs.tag}}
      - name: Push the tagged Docker image
        if:  ${{ steps.vars.outputs.tag != '/master' }}
        run: docker push cscfi/metadata-submitter:${{steps.vars.outputs.tag}}
      - name: Build the latest Docker image
        if:  ${{ steps.vars.outputs.tag == '/master' }}
        run: docker build . --file Dockerfile --tag cscfi/metadata-submitter:latest
      - name: Push the latest Docker image
        if:  ${{ steps.vars.outputs.tag == '/master' }}
        run: docker push cscfi/metadata-submitter:latest
